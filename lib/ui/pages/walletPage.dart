import 'package:flutter/material.dart';
import '../../shared/thame.dart';

class walletPage extends StatelessWidget {
  const walletPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('data wallet'),
    );
  }
}
